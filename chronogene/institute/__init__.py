"""Chronogene Institute — a multi-agent computational research framework.

A Principal Investigator directs a team of researcher agents (grad students and
postdocs), independent checkers, and a literature scanner to do *computational*
epigenetic-aging research on public datasets. The team writes files, checks them,
and holds them; it never publishes, submits, spends, or contacts anyone. Every
claim it produces traces back to a dataset accession and the exact query that
produced it. A human board makes every outward-facing decision through the
Board Console UI (../web).
"""

__version__ = "0.1.0"
